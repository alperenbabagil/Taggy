<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="?attr/colorPrimaryVariant" />

    <!-- The corners of the shape -->
    <corners android:radius="@dimen/done_button_radius"/>

    <stroke android:width="@dimen/cancel_button_stroke_width"
        android:color="?attr/colorOnPrimary"/>

    <!-- Padding for the background, e.g the Text inside a TextView will be
    located differently -->
    <padding android:left="@dimen/button_horizontal_margin" android:right="@dimen/button_horizontal_margin" />

</shape>